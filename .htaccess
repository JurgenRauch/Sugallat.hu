RewriteEngine On

# ===== CANONICAL (REMOVE DUPLICATE PAGE) =====
# The canonical page lives at `/kozbeszerzes-ertekhatar/`.
# If an old/development duplicate path is requested, redirect to the canonical URL.
RewriteRule ^tevekenysegeink/kozbeszerzes-ajanlatkeroknek/kozbeszerzes-ertekhatar/?$ /kozbeszerzes-ertekhatar/ [R=301,L,NC]
RewriteRule ^tevekenysegeink/kozbeszerzes-ajanlatkeroknek/kozbeszerzes-ertekhatar/index\.html$ /kozbeszerzes-ertekhatar/ [R=301,L,NC]

# Canonical contact page (folder route)
RewriteRule ^kapcsolat\.html$ /kapcsolat/ [R=301,L,NC]

# ===== SEO-PRESERVING REDIRECTS FROM OLD PHP SITE =====

# Exact matches - redirect to renamed files
RewriteCond %{QUERY_STRING} ^f=bemutatkozas$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=tevekenysegeink$
RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=hasznos_linkek$
RewriteRule ^index\.php$ /hasznos-linkek.html? [R=301,L]

# Direct matches - redirect to existing files
RewriteCond %{QUERY_STRING} ^f=elerhetoseg$
RewriteRule ^index\.php$ /kapcsolat.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=referenciak$
RewriteRule ^index\.php$ /referenciak.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=arak$
RewriteRule ^index\.php$ /arak.html? [R=301,L]

# Missing pages - redirect to most relevant existing pages
RewriteCond %{QUERY_STRING} ^f=munkatarsaink$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=uzletpolitikank$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=letoltheto_dokumentumok$
RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=partner_cegek$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

# English version redirect
RewriteRule ^index_en\.php$ /pages/en/index.html [R=301,L]

# ===== ADDITIONAL OLD FILE REDIRECTS =====

# Activities and business policy pages
RewriteCond %{QUERY_STRING} ^f=activities$
RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=business_policy$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

# Colleagues/team pages - redirect to introduction
RewriteCond %{QUERY_STRING} ^f=colleagues$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=munkatarsaink_benko_istvan$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=munkatarsaink_karpeta_anna$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=munkatarsaink_szekely_anna_krisztina$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=munkatarsaink_regi$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

# Contact variations
RewriteCond %{QUERY_STRING} ^f=contacts$
RewriteRule ^index\.php$ /kapcsolat.html? [R=301,L]

# Introduction page
RewriteCond %{QUERY_STRING} ^f=introduction$
RewriteRule ^index\.php$ /bemutatkozas.html? [R=301,L]

# References variations
RewriteCond %{QUERY_STRING} ^f=references$
RewriteRule ^index\.php$ /referenciak.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=referenciak_0222$
RewriteRule ^index\.php$ /referenciak.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=referenciak_kornyezetvedelem$
RewriteRule ^index\.php$ /referenciak.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=referenciak_kozbeszerzes$
RewriteRule ^index\.php$ /referenciak.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=referenciak_muszaki_tervezes_ellenorzes$
RewriteRule ^index\.php$ /referenciak.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=referenciak_oktatas$
RewriteRule ^index\.php$ /referenciak.html? [R=301,L]

RewriteCond %{QUERY_STRING} ^f=referenciak_palyazati_tanacsadas$
RewriteRule ^index\.php$ /referenciak.html? [R=301,L]

# Old pricing page
RewriteCond %{QUERY_STRING} ^f=arak_regi$
RewriteRule ^index\.php$ /arak.html? [R=301,L]

# Environmental consulting (Envecon) - redirect to services
# RewriteCond %{QUERY_STRING} ^f=Envecon$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# Public procurement process guide - redirect to services
# RewriteCond %{QUERY_STRING} ^f=Kozbeszerzesi_eljarasok_menete$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# Downloadable documents - various project files redirect to services
# RewriteCond %{QUERY_STRING} ^f=letoltes_acs$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=letoltes_Acs_Bobita_ovoda$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=letoltes_acs_cukorgyari_rendelo$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=letoltes_Acs_rendelo_parkolo$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=letoltes_Diosd_csapadek$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=letoltes_Nagyigmand_tuzolto_laktanya$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=letoltes_Nagyigmand_utepites$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=letoltes_paks$
# RewriteRule ^index\.php$ /tevekenysegeink.html? [R=301,L]

# Generic "Answer" page - redirect to contact
# RewriteCond %{QUERY_STRING} ^f=Answer$
# RewriteRule ^index\.php$ /kapcsolat.html? [R=301,L]

# Temporary files - redirect to homepage
# RewriteCond %{QUERY_STRING} ^f=tmpA6BE$
# RewriteRule ^index\.php$ /? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=tmpAD30$
# RewriteRule ^index\.php$ /? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=tmpDEA8$
# RewriteRule ^index\.php$ /? [R=301,L]

# RewriteCond %{QUERY_STRING} ^f=tmpFB11$
# RewriteRule ^index\.php$ /? [R=301,L]

# Main index.php redirect to homepage
RewriteRule ^index\.php$ / [R=301,L]

# ===== CLEAN URL REDIRECTS (OPTIONAL) =====
# Uncomment these if you want to remove .html extensions in the future

# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteRule ^([^\.]+)$ $1.html [NC,L]

# RewriteCond %{THE_REQUEST} /([^.]+)\.html [NC]
# RewriteRule ^ /%1 [NC,L,R=301]

# ===== ERROR HANDLING =====

# Custom 404 error page
ErrorDocument 404 /404.html

# ===== LOGGING CONFIGURATION =====

# Enable custom logging (if supported by hosting provider)
# LogLevel info
# ErrorLog logs/error.log
# CustomLog logs/access.log combined

# Log 404 errors for monitoring broken links
# RewriteLog logs/rewrite.log
# RewriteLogLevel 2

# ===== SECURITY AND PERFORMANCE =====

# Prevent access to sensitive files
<Files ".htaccess">
    Order allow,deny
    Deny from all
</Files>

<Files "*.log">
    Order allow,deny
    Deny from all
</Files>

# Prevent access to log directory
<Directory "logs">
    Order allow,deny
    Deny from all
</Directory>

# Enable compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>

# Browser caching
<IfModule mod_expires.c>
    ExpiresActive on
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
</IfModule>